[project]
name = "rwa-agent"
version = "0.1.0"
description = "Automated buy-side AI agent for the RWA fundraising platform: analyzes campaigns via fundraising-api, scores risk, and executes real Solana devnet payments to buy RWA tokens."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.5",
    "httpx>=0.27",
    "openai>=1.50",
    "solana>=0.34",
    "solders>=0.21",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "respx>=0.21",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
